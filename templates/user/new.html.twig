{% extends 'base.html.twig' %}

{% block title %}Create New User{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-success">
                        <h2 class="mb-0 text-white">Create New User</h2>
                    </div>
                    <div class="card-body">
                        {{ include('user/_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
